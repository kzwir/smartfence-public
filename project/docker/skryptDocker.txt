docker network create -d bridge mysql-net

// uruchomienie pliku yaml
docker-compose up -d

// kopiowanie pliku z host do docker
$docker cp D:/Projects/plot/smartfence/project/php/php.ini 5722873a55d7:/usr/local/etc/php

// uruchomienie mysqli
// docker run the image, and then install it from inside the container:
docker-php-ext-install mysqli 
docker-php-ext-enable mysqli
apachectl restart
